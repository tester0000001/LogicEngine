package LogicEngine.eng.service;

public enum TokenType {
    LPAREN,
    RPAREN,
    AND,
    OR,
    NOT_EQUALS,
    EQUALS,
    LESS_THAN,
    GREATER_THAN,
    LESS_THAN_OR_EQUALS,
    GREATER_THAN_OR_EQUALS,
    IDENTIFIER,
    STRING,
    NUMBER,
    EOF
}